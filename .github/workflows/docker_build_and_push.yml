name: docker_build_and_push.yml
on:
  workflow_run:
    workflows: ["Run code quality tests"]
    types:
      - completed

env:
  IMAGE_NAME: marethyuu/public_storage
  IMAGE_TAG: "${{ github.event.workflow_run.head_branch }}_${{ github.event.workflow_run.head_sha }}_${{github.run_id}}"

jobs:
  docker:
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: 'go.mod'
      - run: go mod tidy
      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - run: docker compose build
      - run: docker tag $IMAGE_NAME:$IMAGE_TAG $IMAGE_NAME:latest_dev
      - run: docker push $IMAGE_NAME:$IMAGE_TAG
      - run: docker push $IMAGE_NAME:latest_dev

